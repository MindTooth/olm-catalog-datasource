image:
  repository: registry.example/olm-catalog-datasource
  tag: v0.0.0
signaturePolicy:
  existingConfigMap: olm-catalog-policy
registryAuth:
  existingSecret: olm-registry-auth
refreshToken:
  existingSecret: olm-refresh-token
config:
  refreshInterval: 6h
  refreshTimeout: 30m
  parseConcurrency: 2
  channels:
    - "4.22"
